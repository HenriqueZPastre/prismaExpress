name: Fly Deploy
on:
  push:
    branches:
      - main
jobs:
  deploy:
    name: Deploy app
    runs-on: ubuntu-latest
    environment: teste
    steps:
      - uses: actions/checkout@v3
      - uses: superfly/flyctl-actions/setup-flyctl@master
      - run: echo "Deploying to Fly.io ${{vars.DATABASE_URL}}"
      - run: flyctl deploy --build-arg DATABASE_URL="postgres://postgres:U8caWUlrNPXZwZF@postgrestesnew.flycast:5432"  --remote-only 
        env:
          FLY_API_TOKEN: ${{ secrets.FLY_API_TOKEN }}
          